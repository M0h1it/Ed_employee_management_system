<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Required by react-native-vision-camera. RECORD_AUDIO is
         deliberately NOT requested: this app only ever captures still
         photos for face matching, never video or audio, and asking for a
         permission the app has no use for is exactly the kind of over-broad
         request PHASE-3-BRIEF.md's own consent section is careful to avoid
         for camera data — no reason to be less careful about it here. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.front" android:required="true" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="${usesCleartextTraffic}"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:screenOrientation="portrait"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- HOME + DEFAULT, in the SAME intent-filter as MAIN/LAUNCHER
             above, is what lets Android offer this app as a home-screen
             replacement — pressing the physical Home button brings the
             kiosk back to the front instead of an actual home screen.
             This is deliberately NOT Device Owner / Lock Task mode: the
             person explicitly chose this simpler route because Device
             Owner requires a factory reset and a one-time adb command
             before any Google account is added, which was not available
             for this tablet. Recents (the square/overview button) is NOT
             blocked by this, and anyone who goes into system Settings can
             still change the default launcher back — this is a soft lock,
             not a hardware-provisioned one. -->
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.HOME" />
            <category android:name="android.intent.category.DEFAULT" />
        </intent-filter>
      </activity>
    </application>
</manifest>