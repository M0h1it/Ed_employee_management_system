# Copy to .env.local to override. .env.local is gitignored.

# true  -> run on the Phase 1 mock service worker, no backend needed
# false -> talk to the FastAPI server through the Vite proxy (the default)
VITE_USE_MOCKS=false
